/*Need to disable bootJar to avoid error:
	> Execution failed for task ':commons:bootJar'.
	> Error while evaluating property 'mainClass' of task ':commons:bootJar'
*/

bootJar {
    enabled = false
}
